<h1>Iata retetele  pentru <%= @pacient.nume %></h1>

<table>
  <tr>
    <th>Data</th>
    <th>Medicamente</th>
    <th>Retetar</th>
    <th>Pacient</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @pacient.reteta.each do |retetum| %>
  <tr>
    <td><%= retetum.data %></td>
    <td><%= retetum.drugs %></td>
    <td><%= retetum.retetar %></td>
    <td><%= retetum.pacient %></td>
    <td><%= link_to 'Show', pacient_reteta_path(@pacient), :remote=>true %></td>
    <td><%# link_to 'Edit', edit_retetum_path(retetum) %></td>
    <td><%# link_to 'Destroy', retetum, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Mai prescrie o reteta', new_pacient_retetum_path(@pacient) %>
