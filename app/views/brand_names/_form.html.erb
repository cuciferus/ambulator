<%= form_for(@brand_name) do |f| %>
  <% if @brand_name.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brand_name.errors.count, "error") %> prohibited this brand_name from being saved:</h2>

      <ul>
      <% @brand_name.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :drug %><br />
    <%= f.text_field :drug %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
