<%= form_for(@concentration) do |f| %>
  <% if @concentration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@concentration.errors.count, "error") %> prohibited this concentration from being saved:</h2>

      <ul>
      <% @concentration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :concentration %><br />
    <%= f.text_field :concentration %>
  </div>
  <div class="field">
    <%= f.label :drug %><br />
    <%= f.text_field :drug %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
