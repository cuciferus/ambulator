<%= form_for(@drug_concentration) do |f| %>
  <% if @drug_concentration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drug_concentration.errors.count, "error") %> prohibited this drug_concentration from being saved:</h2>

      <ul>
      <% @drug_concentration.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :concentration %><br />
    <%= f.text_field :concentration %>
  </div>
  <div class="field">
    <%= f.label :drug %><br />
    <%= f.text_field :drug %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
