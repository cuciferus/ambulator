<h1>Listing exits</h1>

<table>
  <tr>
    <th>Data</th>
    <th>Motiv</th>
    <th>Pacient</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @exits.each do |exit| %>
  <tr>
    <td><%= exit.data %></td>
    <td><%= exit.motiv %></td>
    <td><%= exit.pacient %></td>
    <td><%= link_to 'Show', exit %></td>
    <td><%= link_to 'Edit', edit_exit_path(exit) %></td>
    <td><%= link_to 'Destroy', exit, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Exit', new_exit_path %>
